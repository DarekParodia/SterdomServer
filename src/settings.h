#pragma once

#include <string>

// look for comments in .cpp file
namespace settings
{
    extern std::string program_name;
    extern bool verbose;
    extern bool debug;
    extern bool no_confirm;
}
